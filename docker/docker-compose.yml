version: "3.8"
services:
  web:
    build: .
    command: python app.py
    ports:
      - "5000:5000"
    volumes:
      - .:/todo
  db:
    image: "mongo:latest"
    ports:
      - "27017:27017"
    command: mongod --port 27017